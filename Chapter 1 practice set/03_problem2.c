#include<stdio.h>

int main(){

    float c=37.0,f=(9.0/5.0*c+32);// we have to put 9.0 not 9 as the answer we getting is wrong
    
    printf("the vale of fahrenheit is %f ",f);
    
    return 0;
}