#include <stdio.h>

int main (){
int a = 6 , b = 3, c = a+b;
printf (" The value of a is %d and value of b is %d and sum is %d\n", a,b,c);

//modulus operator is used to get the remainder

printf("The remainder when a is divided by is %d", a%b);

//this does not work for exponential in c
//int d = a^b; this is not in power kuch or use karte hai
return 0;

}